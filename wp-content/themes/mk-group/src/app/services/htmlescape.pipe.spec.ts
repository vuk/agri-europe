import { HtmlescapePipe } from './htmlescape.pipe';

describe('HtmlescapePipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlescapePipe();
    expect(pipe).toBeTruthy();
  });
});
